Drop reference audio files here (WAV, MP3, OGG, etc.) and register them in src/solo/presets.ts.

Each preset needs: id, title, bpm, pattern (16-slot constrained rhythm), and audioPath (URL under /solo/...).

You are responsible for licensing any commercial recordings you add.
