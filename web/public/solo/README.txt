Drop reference audio files here (WAV, MP3, OGG, etc.) and register them in src/solo/presets.ts.

The default Singleplayer preset loads /solo/WWRY.mp3 (see presets.ts). For another clip, add the file here and set audioPath to "/solo/yourfile.wav" (or .mp3, etc.).

Match preset bpm to your loop so the metronome lines up.

You are responsible for licensing any commercial recordings you add.
